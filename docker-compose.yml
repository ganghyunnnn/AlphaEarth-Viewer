# One-line run:  docker compose up --build
# Browser:       http://localhost:8080
services:
  backend:
    build: ./backend
    expose:
      - "8000"
    volumes:
      - tiles_cache:/cache
    environment:
      - AEF_TILE_CACHE=/cache

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  tiles_cache:
